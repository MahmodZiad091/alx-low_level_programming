#include <stdio.h>

int main()
{
	int n;
	if ( n > 0 )
	{
		printf ("%d is positive \n",n);
		return 0;
	}
	if ( n = 0 )
	{
		printf ("%d is zero \n", n);
		return 0;
	}
	if ( n < 0 )
	{
		printf("%d is negative", n);
		return 0;
	}
	return 0;
}

